import React from 'react'

const CellComponent = ({label}) => {
  return (
    <div>
      <button
      type='button'
      area-label = {label}
      />
    </div>
  )
}

export default CellComponent
